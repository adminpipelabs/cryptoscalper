# Crypto Scalper — 15-min ETH/BTC/SOL Polymarket Bot

# Wallet private key (Polygon, must be onboarded on Polymarket)
PRIVATE_KEY=0x...

# Polygon RPC
RPC_URL=https://polygon-bor-rpc.publicnode.com

# Scalper config
SCALP_BET_SIZE=10         # tokens per bid (at $0.25 each = $2.50 per side)
SCALP_ASSETS=eth,btc      # comma-separated: eth,btc,sol
SCALP_POLL_SECONDS=15     # how often the bot scans (seconds)
SCALP_MIN_TIME_LEFT=300   # min seconds remaining on a window to enter
SCALP_SELL_OFFSET=0.04    # offset for manual sell pricing
SCALP_PORT=8081           # dashboard port
